---
description: Check cyber-skills toolkit install and index health
---

# /cyber-status

1. Check paths:
   - `~/.grok/tools/cyber-skills/index.json`
   - `~/.grok/skills/cyber/SKILL.md`
   - search script exists
2. Report `total_skills`, `version`, `generated_at` from index.json if present.
3. Optional: `git -C ~/.grok/tools/cyber-skills log -1 --oneline`
4. If missing → point to install.ps1 / install.sh.
